#include <stdio.h>

int main()
{
	float f=4.5;
	if (f == 4.5)
		printf("True\n");
	else
		printf("Faslse\n");

	printf("%d\n", sizeof(void));
	return 0;
}
/*
 * 0.1 ==> Double data type
 * The comparision both the operand must be of same data type. 
 * flot has 6 degree of presision, while the in comparision 0.1 is having the 10 degree of presision.
 * */
